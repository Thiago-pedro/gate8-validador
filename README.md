# Gate8 Validador

App da portaria: lê o QR do ingresso e faz o check-in. Mesmo banco do site e do app cliente (`qdflmwogpftalkdrypli`).

```powershell
cd "C:\Users\Thiago Costa\Desktop\Gate8\gate8-validador"
npx expo start
```

No Android, Expo Go na mesma Wi‑Fi. Recarrega com `r`.
